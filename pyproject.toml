[tool.poetry]
name = "simple-time-tracker"
version = "0.1.0"
description = "A command line TUI application for timekeeping that generates invoices based on user input."
authors = ["Your Name <email@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
rich = "^13.7.0"
fpdf2 = "^2.7.9"
playwright = "^1.52.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"